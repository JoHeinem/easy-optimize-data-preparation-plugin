# easy-optimize-data-preparation-plugin

Add the following line to bpm-platform.xml:

```xml
<plugin>
  <class>org.camunda.bpm.platform.plugin.optimize.data.preparation.EasyOptimizeDataPreparationPlugin</class>
</plugin>
```
